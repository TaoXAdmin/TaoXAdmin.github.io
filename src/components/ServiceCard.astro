---
interface Props {
  title: string;
  body?: string;
  bullets?: string[];
}
const { title, body, bullets = [] } = Astro.props as Props;
---
<article class="rounded-2xl shadow p-4 bg-white dark:bg-neutral-900">
  <h3 class="text-lg font-semibold">{title}</h3>
  {body && <p class="mt-2 text-neutral-700 dark:text-neutral-300">{body}</p>}
  {bullets.length > 0 && (
    <ul class="mt-3 list-disc pl-5 space-y-1 text-neutral-700 dark:text-neutral-300">
      {bullets.map((item: string) => <li>{item}</li>)}
    </ul>
  )}
</article>
